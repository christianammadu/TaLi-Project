<!-- GENERATED — edit .claude/skills/groundwork/ instead. Synced by sync-from-dev.mjs. -->
# Add a settings mechanism plus onboarding that captures display name and usage type (business/personal) so welcomes are personalized and business users can record business info; decide whether settings live on an authenticated web page or via WhatsApp commands. — tracking

Work packages, dependencies, critical path. Source of truth for execution status (survives sessions).

> Only the orchestrator writes the status checkboxes below. WP owners report Definition-of-Done back; the orchestrator records the result.

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

<!-- groundwork:auto:start round-fold -->
<!-- last_action: review · 2026-06-05 -->
_No review passes yet. After `groundwork review` runs, this block carries a one-paragraph summary of what the most recent Round changed in this doc + where (e.g. "Round 7 folded six P1 gaps in: G1 aspect-ratio (§2/§5/§6/§7/§13), G2 capability matrix (§5/§6/§13)...")._
<!-- groundwork:auto:end round-fold -->

---

## Build status (orchestrator-owned)

Kickoff: 2026-06-05. Decisions: onboarding progress → `onboarding_step` column; name → skippable. Live `alembic`/`pytest` deferred to a final sweep (user-approved — no venv/MySQL in this env).

- [x] **WP-01** — `User` + migration `0003` (usage_type · business_profile · onboarding_step). Offline-verified; **live alembic up/down deferred** → final sweep.
- [x] **WP-02** — `app/auth.py` setters: set_display_name · set_usage_type · update_business_profile · get/set_onboarding_state.
- [x] **WP-03** — onboarding gate + state machine in `app/web/routes.py` (resumable; name skippable; D-01 copy).
- [x] **WP-04** — `settings` command + `set name|currency|type|business` in `routes.py`.
- [x] **WP-05** — `handle_access_code` greets by name for returning users; new users routed into onboarding.
- [x] **WP-06** — `tests/test_onboarding_settings.py` (23 tests, mock-based, DB-free).
- [x] **G-SCHEMA** — columns land two ways: Alembic `0003` *and* an idempotent self-heal in `init_db()` (the live app provisions via `init_db`, not Alembic — restart adds the columns). Resolved the live `Unknown column 'users.usage_type'` registration error.

**Post-test fixes (2026-06-05):** (1) `init_db()` self-heals the 3 new columns on boot; (2) OTP paste in `register.html` now distributes a pasted/autofilled code across all 6 boxes; (3) `agent_1_intake`/`agent_3_cfo` no longer return raw JSON to users — unparseable input (e.g. "Hi") gets a friendly nudge, and escalations return their human `message`.

**Final verification (you run):** `python -m venv venv && source venv/bin/activate && pip install -r requirements.txt`, ensure MySQL is up + `tali` DB exists, then `alembic upgrade head` · `alembic downgrade -1` · `alembic upgrade head` · `python -m pytest`.

---

## Critical path

_Hand-written. The 1-line execution flow capturing the linear bottleneck through Phase 1._

```
schema migration (G-SCHEMA)  →  auth setters  →  onboarding gate  →  personalized welcome  →  tests
```

**Parallel track** that can start once `auth setters` (WP-02) lands:
- `settings` command + `set …` edits (WP-04) — runs alongside the onboarding gate (WP-03); both share `app/web/routes.py`, so same owner / sequential edits within that file.

---

## How to use this file

- Each section below is one work package — its goal, files, deps, DoD.
- The matrix + wave plan + critical path are generated by `groundwork orchestrate` from these sections.
- Add a WP: write a `## WP-NN — title` section; orchestrate picks it up on next run.

## Work-package matrix

<!-- groundwork:auto:start wp-matrix -->
<!-- last_action: orchestrate · 2026-06-05T11:49:04Z -->
| WP | Title | Repo | Depends on | Parallel with | Output |
|---|---|---|---|---|---|
| **WP-01** | Schema migration (`usage_type` + `business_profile`) | bookkeeper | — | — | Alembic `0003` + **G-SCHEMA** locked |
| **WP-02** | Auth/data setters + onboarding accessors | bookkeeper | WP-01 | — | setter API in `app/auth.py` |
| **WP-03** | Onboarding state machine + gate | bookkeeper | WP-02 | WP-04 (shared `routes.py`) | conversational onboarding · D-01 |
| **WP-04** | `settings` command + `set …` edits | bookkeeper | WP-02 | WP-03 (shared `routes.py`) | in-chat settings surface · D-01 |
| **WP-05** | Personalized welcome wiring | bookkeeper | WP-03 | WP-06 | greet-by-name |
| **WP-06** | Tests | bookkeeper | WP-03, WP-04 | WP-05 | pytest coverage |
<!-- groundwork:auto:end wp-matrix -->

## Wave plan

<!-- groundwork:auto:start wave-plan -->
<!-- last_action: orchestrate · 2026-06-05T11:49:04Z -->
- **Wave 0 (gate)** — **WP-01** schema migration. Locks **G-SCHEMA** (the `usage_type` enum + `business_profile` JSON shape). Everything downstream reads this, so it is sequential and first.
- **Wave 1** — **WP-02** auth/data setters. Single owner; unblocks the two surface WPs.
- **Wave 2 (fan-out, file-coordinated)** — **WP-03** onboarding gate + **WP-04** `settings` command. Logically parallel, but **both edit `app/web/routes.py`** — so one owner does them sequentially (or split by function with care). Both cite locked design **D-01**.
- **Wave 3** — **WP-05** personalized welcome (needs the captured name from WP-03) + **WP-06** tests (needs WP-03 + WP-04). Parallel to each other.
<!-- groundwork:auto:end wave-plan -->

## Critical-path graph

<!-- groundwork:auto:start critical-path -->
<!-- last_action: orchestrate · 2026-06-05T11:49:05Z -->
```
WP-01 ──▶ WP-02 ──┬──▶ WP-03 ──┬──▶ WP-05
        (G-SCHEMA)│            └──▶ WP-06
                  └──▶ WP-04 ──────▶ WP-06
```

Longest chain (the bottleneck): **WP-01 → WP-02 → WP-03 → WP-06** (4 deep). WP-04 joins WP-03 at Wave 2; WP-05 and WP-06 close out Wave 3. The only shared-file contention is `app/web/routes.py` between WP-03 and WP-04 — resolved by single ownership of that file in Wave 2.
<!-- groundwork:auto:end critical-path -->

---

## Work packages, grouped by phase

> WPs are grouped under `## Phase N — title` headers. Use `(sketch)` on phase headers whose contents aren't fully detailed yet — convention: current phase = fully detailed, next phase = sketched (scope only), later phases = stubbed (one paragraph). The orchestrate action reads `### WP-NN` headings regardless of phase group; the grouping is for human comprehension. **Cross-link sub-plans** (diff-plans / decision-docs / bug-docs) from the relevant WP with `- **Sub-plan:** [NN-slug.md](NN-slug.md)`.

## Phase 1 — Onboarding + name capture + usage type (in-chat, text-first)

### WP-01 — Schema migration (usage_type + business_profile)

- **Goal:** Add the columns onboarding/settings read & write, and lock the storage shape (Option A).
- **Repo:** bookkeeper
- **Branch:** feature/settings-and-onboarding
- **Depends on:** —
- **Files (create/touch):** `app/data/models.py` (extend `User`); `migrations/versions/0003_*.py` (create).
- **Consumes / Produces:** Produces **G-SCHEMA** (the frozen `usage_type` enum + `business_profile` JSON + onboarding marker). Everything downstream reads this shape.
- **Do-not-touch:** the orphaned `business_id` columns (stay NULL — do not revive the `DEFAULT 1` bucket); `init_db()` DDL (schema goes through Alembic).
- **DoD (self-verifiable):** `alembic upgrade head` adds `users.usage_type ENUM('personal','business') NULL`, `users.business_profile JSON NULL`, and an onboarding-progress marker; `alembic downgrade -1` reverses cleanly; `app/data/models.py` matches.

### WP-02 — Auth/data setters + onboarding-progress accessors

- **Goal:** The read/write surface for name, usage type, business profile, and onboarding progress.
- **Repo:** bookkeeper
- **Branch:** feature/settings-and-onboarding
- **Depends on:** WP-01 (G-SCHEMA)
- **Files (create/touch):** `app/auth.py` (add `set_display_name`, `set_usage_type`, `update_business_profile`, onboarding get/set; extend `register_user` if needed).
- **Consumes / Produces:** Consumes G-SCHEMA. Produces the setter API used by WP-03/WP-04. Session dict already carries the fields (`get_active_session`).
- **Do-not-touch:** existing auth/session flow signatures (keep return shapes stable for routes/agent_router).
- **DoD (self-verifiable):** each setter persists and is unit-tested in isolation; `display_name` written by `set_display_name` is read back by `get_active_session`.

### WP-03 — Onboarding state machine + gate

- **Goal:** First-run conversational onboarding: name → personal/business → (if business) business name + category; resumable across messages.
- **Repo:** bookkeeper
- **Branch:** feature/settings-and-onboarding
- **Depends on:** WP-02
- **Files (create/touch):** `app/web/routes.py` (onboarding gate before AgentRouter + `handle_onboarding_*`).
- **Design reference:** `designs/onboarding-settings-a-conversational.html` (D-01).
- **Consumes / Produces:** Consumes WP-02 setters. Produces a completed user profile that unlocks normal use.
- **Do-not-touch:** the AgentRouter pipeline; the `settings` handlers (WP-04's surface) — coordinate edits to the shared file.
- **DoD (self-verifiable):** a user with `display_name IS NULL` is gated into onboarding on next authenticated message; answering name sets it; business branch collects name + category into `business_profile`; sending an unrelated message mid-flow re-prompts the next unanswered question (no data loss).

### WP-04 — `settings` command + `set …` edits

- **Goal:** In-chat settings menu + single-field edits (text-first).
- **Repo:** bookkeeper
- **Branch:** feature/settings-and-onboarding
- **Depends on:** WP-02
- **Files (create/touch):** `app/web/routes.py` (`handle_settings`; `set name/currency/type/business` parsing; extend `AUTH_COMMANDS`).
- **Design reference:** `designs/onboarding-settings-a-conversational.html` (D-01).
- **Consumes / Produces:** Consumes WP-02 setters. Produces the editable settings surface.
- **Do-not-touch:** the onboarding gate (WP-03) — shares `routes.py`; same owner or sequential edits.
- **DoD (self-verifiable):** `settings` prints the menu; `set currency USD`, `set name X`, `set type personal` each persist + echo confirmation; invalid input rejected gracefully.

### WP-05 — Personalized welcome wiring

- **Goal:** Make the welcome greet by name once captured (light up the dead `routes.py:69` path).
- **Repo:** bookkeeper
- **Branch:** feature/settings-and-onboarding
- **Depends on:** WP-03
- **Files (create/touch):** `app/web/routes.py` (`handle_access_code` welcome + first-login welcome).
- **Consumes / Produces:** Consumes the captured `display_name`. Produces the personalized greeting.
- **Do-not-touch:** unrelated reply copy.
- **DoD (self-verifiable):** after onboarding, login welcome reads "Welcome, Ada!" (not the phone number).

### WP-06 — Tests

- **Goal:** Lock the onboarding state machine + settings edits with unit tests.
- **Repo:** bookkeeper
- **Branch:** feature/settings-and-onboarding
- **Depends on:** WP-03, WP-04
- **Files (create/touch):** `tests/` (new test modules).
- **Consumes / Produces:** Consumes WP-03/WP-04 behavior. Produces regression coverage.
- **Do-not-touch:** —
- **DoD (self-verifiable):** `python -m pytest` green, covering onboarding resume + each `set …` edit + business branch.

## Phase 2 — Interactive menus + richer business profile (sketch)

_Sketched. Upgrade text menus to native interactive UX (`send_buttons()`/`send_list()` in `app/web/whatsapp.py`, interactive-reply handling in `routes.py`), optionally a Meta-reviewed WhatsApp Flow for the business-profile form, and promotion to a real `businesses` table if multi-business is wanted. Design target: `designs/onboarding-settings-b-interactive.html` (D-02). Open question: is a Flow worth the review overhead vs. a button/text sequence?_

## Phase 3 — Settings depth & web parity (stub)

_Stub. Per-business categories, notification preferences, language/locale, data export, and (only if demand appears) a read-only authenticated web settings view. Out of scope until Phases 1–2 prove the in-chat surface._
