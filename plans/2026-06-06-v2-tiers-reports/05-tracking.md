<!-- GENERATED — edit .claude/skills/groundwork/ instead. Synced by sync-from-dev.mjs. -->
# Ship TaLi v2: full report suite (P&L, category, inventory valuation, debt aging) + subscription tiers (Free/Pro/Business), Paystack paywall, rate limiting, and scheduled reports — within PythonAnywhere free-tier limits. — tracking

Work packages, dependencies, critical path. Source of truth for execution status (survives sessions).

> Only the orchestrator writes the status checkboxes below. WP owners report Definition-of-Done back; the orchestrator records the result.

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

<!-- groundwork:auto:start round-fold -->
<!-- last_action: review · 2026-06-06 -->
_No review passes yet. After `groundwork review` runs, this block carries a one-paragraph summary of what the most recent Round changed in this doc + where (e.g. "Round 7 folded six P1 gaps in: G1 aspect-ratio (§2/§5/§6/§7/§13), G2 capability matrix (§5/§6/§13)...")._
<!-- groundwork:auto:end round-fold -->

---

## Critical path

_Hand-written. The 1-line execution flow capturing the linear bottleneck. Update by hand when the critical path changes; re-derive at the start of each phase._

```
G-TIER-SCHEMA → entitlements → gating+rate-limit / Paystack paywall ┐
G-REPORT-SCHEMA → report suite (P&L/category/inventory/debt) ───────┴→ scheduled reports → rollout
```

**Parallel tracks** (two independent gates open at the start):
- **Report-suite track** — once `G-REPORT-SCHEMA` (WP-01) lands, WP-02..05 fan out in parallel.
- **Monetization track** — once `G-TIER-SCHEMA` (WP-06) lands, entitlements (WP-07) unblocks
  gating (WP-08), upsell (WP-09), payments (WP-10/11), expiry (WP-12), schedules (WP-13).
- Both tracks converge at **WP-14** (scheduled-report runner needs the suite + entitlements + schedules).
- **Phase 2c/2d are decision-gated** on PA hosting + payment provider (see `04-discussion.md`).

---

## How to use this file

- Each section below is one work package — its goal, files, deps, DoD.
- The matrix + wave plan + critical path are generated by `groundwork orchestrate` from these sections.
- Add a WP: write a `## WP-NN — title` section; orchestrate picks it up on next run.

## Work-package matrix

<!-- groundwork:auto:start wp-matrix -->
<!-- last_action: orchestrate · 2026-06-06T16:45:47Z -->
| WP | Title | Repo / Branch | Depends on | Parallel with | Output |
|---|---|---|---|---|---|
| **WP-01** | Report-schema freeze gate (G-REPORT-SCHEMA) | bookkeeper · v2/report-schema | — | WP-06 | frozen `report_type` enum + render dispatch |
| WP-02 | Profit & Loss / income statement | bookkeeper · v2/report-pnl | WP-01 | WP-03, WP-04, WP-05 | `query_pnl` + PDF/XLSX |
| WP-03 | Category breakdown | bookkeeper · v2/report-category | WP-01 | WP-02, WP-04, WP-05 | `query_category_breakdown` + PDF/XLSX |
| WP-04 | Inventory valuation | bookkeeper · v2/report-inventory | WP-01 | WP-02, WP-03, WP-05 | `query_inventory_valuation` + PDF/XLSX |
| WP-05 | Debt / receivables aging | bookkeeper · v2/report-debt-aging | WP-01 | WP-02, WP-03, WP-04 | `query_debt_aging` + PDF/XLSX |
| **WP-06** | Tier & usage schema freeze gate (G-TIER-SCHEMA) | bookkeeper · v2/tier-schema | — | WP-01 | plan/usage/schedule tables + models |
| WP-07 | Entitlements module | bookkeeper · v2/entitlements | WP-06 | WP-10, WP-13 | `entitlements.py` + `can()` |
| WP-08 | Gating + rate-limit enforcement | bookkeeper · v2/gating | WP-07 | WP-09, WP-11, WP-12 | gate check + usage counters |
| WP-09 | Manual upgrade + upsell copy | bookkeeper · v2/manual-upgrade | WP-07 | WP-08, WP-11, WP-12 | testable upgrade path + upsell |
| WP-10 | Paystack payments service | bookkeeper · v2/payments | WP-06 | WP-07, WP-13 | `payments.py` init + verify |
| WP-11 | Paystack webhook + activation | bookkeeper · v2/payment-webhook | WP-10, WP-07 | WP-08, WP-09, WP-12 | signed webhook → plan activation |
| WP-12 | Subscription lifecycle expiry | bookkeeper · v2/sub-expiry | WP-07 | WP-08, WP-09, WP-11 | expiry task + downgrade |
| WP-13 | Report schedules | bookkeeper · v2/schedules | WP-06 | WP-07, WP-10 | `report_schedules` + commands |
| WP-14 | Scheduled-report runner + rollout | bookkeeper · v2/scheduler | WP-13, WP-02..05, WP-07 | — | PA task runner + e2e rollout |
<!-- groundwork:auto:end wp-matrix -->

## Wave plan

<!-- groundwork:auto:start wave-plan -->
<!-- last_action: orchestrate · 2026-06-06T16:46:22Z -->
Two independent freeze gates open the build, so Wave 0 carries both; everything fans out from there.

- **Wave 0 — gates (do first, independent of each other):**
  - `WP-01` Report-schema freeze → **G-REPORT-SCHEMA**
  - `WP-06` Tier & usage schema freeze → **G-TIER-SCHEMA**
- **Wave 1 — post-gate fan-out (parallel):**
  - after G-REPORT-SCHEMA: `WP-02`, `WP-03`, `WP-04`, `WP-05` (the four report kinds)
  - after G-TIER-SCHEMA: `WP-07` (entitlements), `WP-10` (Paystack service), `WP-13` (schedules)
- **Wave 2 — depend on entitlements / payments (parallel):**
  - `WP-08` gating+rate-limit, `WP-09` manual-upgrade+upsell, `WP-12` expiry (need `WP-07`)
  - `WP-11` payment webhook+activation (needs `WP-10`+`WP-07`)
- **Wave 3 — convergence:**
  - `WP-14` scheduled-report runner + rollout (needs the report suite `WP-02..05` + `WP-07` + `WP-13`)

> Decision gate: Wave-1 `WP-10` and Wave-2 `WP-11`, plus the scheduled-report side of `WP-14`,
> are **blocked on the PA-hosting + payment-provider decisions** (`04-discussion.md`). Phases 2a/2b
> (WP-01..09, WP-13) can run on the free tier without those answers.
<!-- groundwork:auto:end wave-plan -->

## Critical-path graph

<!-- groundwork:auto:start critical-path -->
<!-- last_action: orchestrate · 2026-06-06T16:46:22Z -->
Computed from the dependency edges (longest chain). Two gate-rooted chains run in parallel and
converge at `WP-14`:

```
WP-06 (G-TIER-SCHEMA) ─▶ WP-07 ─▶ WP-08 / WP-09 / WP-12
                          │           WP-10 ─▶ WP-11
                          └────────────────────────────▶ WP-14  ◀── convergence
WP-01 (G-REPORT-SCHEMA) ─▶ WP-02 / WP-03 / WP-04 / WP-05 ─────────▶ WP-14
                                                  WP-13 ──────────▶ WP-14
```

**Longest path (4 nodes):** `WP-06 → WP-07 → WP-14` (equiv. `WP-01 → WP-{02..05} → WP-14`,
`WP-06 → WP-13 → WP-14`). Critical bottleneck = whichever of {report suite, entitlements,
schedules} finishes last before the `WP-14` runner. The payment chain
`WP-06 → WP-10 → WP-11` (3 nodes) is off the critical path but decision-gated.
<!-- groundwork:auto:end critical-path -->

---

## Work packages, grouped by phase

> WPs are grouped under `## Phase N — title` headers. Use `(sketch)` on phase headers whose contents aren't fully detailed yet — convention: current phase = fully detailed, next phase = sketched (scope only), later phases = stubbed (one paragraph). The orchestrate action reads `### WP-NN` headings regardless of phase group; the grouping is for human comprehension. **Cross-link sub-plans** (diff-plans / decision-docs / bug-docs) from the relevant WP with `- **Sub-plan:** [NN-slug.md](NN-slug.md)`.

## Phase 2a — Full report suite

### WP-01 — Report-schema freeze gate (G-REPORT-SCHEMA)

- **Goal:** Extend the statement contract so the four new report kinds can fan out: add
  `pnl|category|inventory|debt` to `StatementModel.report_type`, define the
  `report_renderer.render(kind, …)` kind set, and the `statement_agent` dispatch map + titles.
- **Repo:** bookkeeper · **Branch:** v2/report-schema
- **Depends on:** — (Wave 0 gate)
- **Files:** `app/services/validators.py`, `app/services/nlp.py` (enum + base rule), `app/services/report_renderer.py` (dispatch stubs), `app/agents/statement_agent.py`
- **Produces:** the frozen `report_type` enum + render dispatch the suite WPs consume.
- **Gate:** **G-REPORT-SCHEMA** — lock before WP-02..05, WP-14.
- **DoD:** the four new kinds parse via `StatementModel`; `render()` dispatches (stub) each kind without error; existing transactions/cashflow still render.

### WP-02 — Profit & Loss / income statement

- **Goal:** Revenue, expenses-by-category, net profit per currency/period as PDF+Excel.
- **Repo:** bookkeeper · **Branch:** v2/report-pnl · **Depends on:** WP-01 (G-REPORT-SCHEMA)
- **Files:** `app/data/queries.py` (`query_pnl`), `app/services/report_renderer.py` (`_render_pnl_*`), `app/services/nlp.py` (example)
- **DoD:** unit-renders valid PDF+XLSX from sample rows; net profit reconciles with `query_balance`.

### WP-03 — Category breakdown

- **Goal:** Income & expense by category with % of total + top categories.
- **Repo:** bookkeeper · **Branch:** v2/report-category · **Depends on:** WP-01
- **Files:** `app/data/queries.py` (`query_category_breakdown`), `report_renderer.py`, `nlp.py`
- **DoD:** unit-renders both formats; category totals reconcile with `query_sum`.

### WP-04 — Inventory valuation

- **Goal:** Current stock per item (running balance from `inventory_movements`) × last cost, total value, low-stock flags.
- **Repo:** bookkeeper · **Branch:** v2/report-inventory · **Depends on:** WP-01
- **Files:** `app/data/queries.py` (`query_inventory_valuation`), `report_renderer.py`, `nlp.py`
- **DoD:** stock levels match the live ledger guard logic; renders both formats.

### WP-05 — Debt / receivables aging

- **Goal:** Outstanding per person bucketed 0–30 / 31–60 / 61–90 / 90+ days from `debt_logs`/`debt_balances`.
- **Repo:** bookkeeper · **Branch:** v2/report-debt-aging · **Depends on:** WP-01
- **Files:** `app/data/queries.py` (`query_debt_aging`), `report_renderer.py`, `nlp.py`
- **DoD:** aging buckets sum to total outstanding; renders both formats.

## Phase 2b — Tiers + gating + rate limiting

### WP-06 — Tier & usage schema freeze gate (G-TIER-SCHEMA)

- **Goal:** Add plan/usage/schedule tables: `users.plan/plan_status/plan_started_at/plan_expires_at`,
  `subscriptions`, `usage_counters`, `report_schedules` — via startup DDL (project's schema-ownership rule) + ORM models.
- **Repo:** bookkeeper · **Branch:** v2/tier-schema · **Depends on:** — (Wave 0 gate)
- **Files:** `app/data/database.py`, `app/data/models.py`
- **Gate:** **G-TIER-SCHEMA** — lock before WP-07, WP-10..13.
- **DoD:** DDL applies on a scratch DB; models import; columns/tables present; defaults sane (`plan='free'`).

### WP-07 — Entitlements module

- **Goal:** Single source of truth: tier→capability matrix + `can(user, capability) -> (bool, reason)`.
- **Repo:** bookkeeper · **Branch:** v2/entitlements · **Depends on:** WP-06
- **Files:** `app/services/entitlements.py` *(new)*
- **DoD:** unit matrix (free/pro/business) returns expected allow/deny for every capability.

### WP-08 — Gating + rate-limit enforcement

- **Goal:** Enforce `can()` before report generation; per-user/day `usage_counters` for reports + NLP calls; friendly limit message.
- **Repo:** bookkeeper · **Branch:** v2/gating · **Depends on:** WP-07
- **Files:** `app/agents/statement_agent.py`, `app/agents/agent_1_intake.py`, `app/services/entitlements.py` (counters)
- **DoD:** gated request denied with upsell; over-cap returns limit message; counter resets by date key.

### WP-09 — Manual upgrade path + upsell copy

- **Goal:** Admin/manual `plan` upgrade (so the flow is testable without payments) + the WhatsApp upsell message.
- **Repo:** bookkeeper · **Branch:** v2/manual-upgrade · **Depends on:** WP-07
- **Files:** `app/web/routes.py` (admin/`set plan`), `app/services/entitlements.py`
- **DoD:** manually upgrading a user flips entitlements live; upsell copy renders for a denied capability.

## Phase 2c — Paywall / Paystack (sketch)

### WP-10 — Paystack payments service

- **Goal:** Init a transaction → payment link for a chosen plan; verify a transaction by reference.
- **Repo:** bookkeeper · **Branch:** v2/payments · **Depends on:** WP-06
- **Files:** `app/services/payments.py` *(new)*
- **Open:** PA-hosting + provider decision (`04`). **DoD:** test-mode init returns a link; verify confirms a paid reference.

### WP-11 — Paystack webhook + activation

- **Goal:** `POST /payments/paystack/webhook` — HMAC-SHA512 signature verify, on `charge.success` activate plan (+30d), send WhatsApp confirmation; idempotent on `reference`.
- **Repo:** bookkeeper · **Branch:** v2/payment-webhook · **Depends on:** WP-10, WP-07
- **Files:** `app/web/routes.py` or `payment_routes.py` *(new)*, `app/services/entitlements.py` (activate)
- **DoD:** test-mode webhook upgrades plan; replay is idempotent; tampered body rejected.

### WP-12 — Subscription lifecycle expiry

- **Goal:** Expire lapsed plans (`plan_expires_at < now → expired`, downgrade entitlements).
- **Repo:** bookkeeper · **Branch:** v2/sub-expiry · **Depends on:** WP-07
- **Files:** `app/services/entitlements.py`, `scripts/expire_subscriptions.py` *(new, PA task)*
- **DoD:** a lapsed user is downgraded; entitlements reflect it.

## Phase 2d — Scheduled / automated reports (sketch)

### WP-13 — Report schedules

- **Goal:** `report_schedules` rows (user, report_type, cadence, format, next_run_at, active) + create/list over WhatsApp.
- **Repo:** bookkeeper · **Branch:** v2/schedules · **Depends on:** WP-06
- **Files:** `app/data/models.py` (model already in G-TIER-SCHEMA), `app/agents/statement_agent.py` (schedule commands)
- **DoD:** a user can create/list a schedule; rows persist with a correct `next_run_at`.

### WP-14 — Scheduled-report runner + rollout

- **Goal:** PA scheduled task that finds due schedules, generates via StatementAgent, delivers, advances `next_run_at`; cadence gated by tier; end-to-end rollout verification across phases.
- **Repo:** bookkeeper · **Branch:** v2/scheduler · **Depends on:** WP-13, WP-02, WP-03, WP-04, WP-05, WP-07
- **Files:** `scripts/run_scheduled_reports.py` *(new)*
- **DoD:** dry-run generates due reports + advances `next_run_at`; free users get none; one batched daily run fits the free-tier task budget.
