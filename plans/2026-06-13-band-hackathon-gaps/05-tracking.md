<!-- GENERATED — edit .claude/skills/groundwork/ instead. Synced by sync-from-dev.mjs. -->
# Bridge the Band of Agents hackathon gaps — tracking

Work packages, dependencies, critical path. Source of truth for execution status (survives sessions).

> Only the orchestrator writes the status checkboxes below. WP owners report Definition-of-Done back; the orchestrator records the result.

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

<!-- groundwork:auto:start round-fold -->
<!-- last_action: review · 2026-06-13T13:22:09Z -->
Round 2 (2026-06-13) folded review findings into this doc: tightened **WP-02** DoD (vertical reply round-trip + time-box + REST-default fallback — G-05/G-06/G-07), **WP-04** DoD (two-phase propose→review→commit — G-14), **WP-06** (broaden grep to `AgentRouter` callers + add `transaction_agent.py` — G-09; test migration — G-08), **WP-11** (deploy process model + `agent_setup.md` rewrite — G-12/G-13); added a green-test gate to verification. Round 1 authored the original WP-01..WP-11 + critical path.
<!-- groundwork:auto:end round-fold -->

---

## Critical path

_Hand-written. The 1-line execution flow capturing the linear bottleneck through Phase 1/2._

```
G-MODEL-ROUTER (WP-01) ┐
                       ├─▶ port Intake/Ledger/CFO (WP-03/04/05) ─▶ retire broker + gateway (WP-06) ─▶ Compliance review loop (WP-07) ─▶ human-in-loop + audit (WP-08/09) ─▶ demo (WP-11)
G-BAND-CONTRACT (WP-02)┘
```

**Parallel tracks** that can start once the gates land:
- Model router (WP-01) and Band spike (WP-02) are independent — both run in Wave 0.
- Audit surfacing (WP-09) + FinOps (WP-10) can develop against the existing tables in parallel with the agent ports, integrating once agents are on Band.

---

## How to use this file

- Each section below is one work package — its goal, files, deps, DoD.
- The matrix + wave plan + critical path are generated by `groundwork orchestrate` from these sections.
- Add a WP: write a `## WP-NN — title` section; orchestrate picks it up on next run.

## Work-package matrix

<!-- groundwork:auto:start wp-matrix -->
<!-- last_action: orchestrate · 2026-06-13T13:05:34Z -->
| WP | Title | Wave | Depends on | Output |
|---|---|---|---|---|
| WP-01 | Multi-provider model router | 0 | — | `G-MODEL-ROUTER`; `app/services/model_router.py` |
| WP-02 | Band integration spike + connector contract | 0 | — | `G-BAND-CONTRACT`; `app/agents/band/band_client.py` |
| WP-03 | Port Intake onto Band | 1 | G-BAND-CONTRACT, G-MODEL-ROUTER | ported `agent_1_intake.py` |
| WP-04 | Port Ledger + proposed-write envelope | 1 | G-BAND-CONTRACT, G-MODEL-ROUTER | ported `agent_2_ledger.py` |
| WP-05 | Port CFO onto Band | 1 | G-BAND-CONTRACT, G-MODEL-ROUTER | ported `agent_3_cfo.py` |
| WP-06 | Webhook→room gateway; delete broker | 2 | WP-03, WP-04, WP-05 | gateway; `band_sdk.py` removed |
| WP-07 | Compliance/Reviewer agent | 2 | WP-04, G-MODEL-ROUTER | `app/agents/compliance_agent.py` |
| WP-08 | Human-in-the-loop approval in room | 3 | WP-06 | room approval flow |
| WP-09 | Unified audit-trail surfacing | 3 | WP-06 | `app/services/audit.py` + view |
| WP-10 | FinOps per-model cost attribution | 4 | WP-01, WP-09 | finops view |
| WP-11 | Deploy, public repo, demo video | 4 | WP-06, WP-07, WP-08, WP-09 | submission |

Full per-WP briefs: `09-orchestration.md`.
<!-- groundwork:auto:end wp-matrix -->

## Wave plan

<!-- groundwork:auto:start wave-plan -->
<!-- last_action: orchestrate · 2026-06-13T13:05:34Z -->
- **Wave 0 — Freeze gates (parallel):** WP-01 (→`G-MODEL-ROUTER`), WP-02 (→`G-BAND-CONTRACT`). Independent of each other; both land before Wave 1.
- **Wave 1 — Port agents (parallel, disjoint files):** WP-03, WP-04, WP-05 — gated by both freeze gates; worktrees recommended.
- **Wave 2:** WP-06 (gateway; needs all three ports) ‖ WP-07 (Compliance; needs WP-04's envelope).
- **Wave 3:** WP-08 (human-in-loop) ‖ WP-09 (audit) — both need the room gateway (WP-06).
- **Wave 4:** WP-10 (FinOps; needs WP-01 + WP-09) → WP-11 (submission; pulls in whatever differentiators landed).

MVP cut line: **Waves 0–2 (WP-01..WP-07)** clear the disqualifying gap + add the review loop. Waves 3–4 are independently droppable differentiators + polish.
<!-- groundwork:auto:end wave-plan -->

## Critical-path graph

<!-- groundwork:auto:start critical-path -->
<!-- last_action: orchestrate · 2026-06-13T13:06:25Z -->
Computed from the dependency edges in `.groundwork.json.ids`:

```
WP-01 (G-MODEL-ROUTER) ─┐
                        ├─▶ WP-03 ─┐
WP-02 (G-BAND-CONTRACT)─┤   WP-04 ─┼─▶ WP-06 ─┬─▶ WP-08 ─┐
                        └─▶ WP-05 ─┘   │      └─▶ WP-09 ─┼─▶ WP-10 ─▶ WP-11
                            WP-04 ─────┴─▶ WP-07 ────────┘
```

Longest chains (tie): `WP-02 → WP-04 → WP-06 → WP-09 → WP-10 → WP-11` and `WP-01 → WP-04 → WP-06 → WP-09 → WP-10 → WP-11`. WP-02 (Band connector) and WP-04 (Ledger port) are the bottleneck nodes — start both first.
<!-- groundwork:auto:end critical-path -->

---

## Work packages, grouped by phase

> WPs grouped under `## Phase N` headers. The orchestrate action reads `### WP-NN` headings regardless of phase group.

## Phase 1 — Foundations & contracts

### WP-01 — Multi-provider model router (`get_client(role)`)

- **Status:** `[x]` **done** (2026-06-14) — `model_router.py` + routed `nlp.py`; 8/8 router tests green. `G-MODEL-ROUTER` frozen. Follow-up: `ai_logs.model_name` still hard-coded in `agent_1_intake.py` (fixed in WP-03).
- **Goal:** Generalize `get_openai_client()` into a role-keyed factory that routes across AI/ML API (frontier), Featherless (OS workers), and OpenAI (fallback). Freezes `G-MODEL-ROUTER`.
- **Repo:** tali
- **Branch:** `feat/model-router`
- **Depends on:** —
- **Files (create/touch):** create `app/services/model_router.py`; touch `app/services/nlp.py` (`:7`, `:197`), `app/config.py`, `.env.example`.
- **Consumes / Produces:** Produces `G-MODEL-ROUTER` (the `get_client(role)` signature + role→provider map + fallback policy).
- **Do-not-touch:** agent files (they adopt the router in Phase 2), `band_sdk.py`.
- **DoD (self-verifiable):** unit test — `get_client("intake")`→Featherless, `get_client("cfo")`→AI/ML, forced primary-provider error→OpenAI fallback; `ai_logs` records the real provider+model+cost (Phase 1 verification #1–2).

### WP-02 — Band integration spike + connector contract

- **Status:** `[~]` **in progress** — connector seam + fire-and-forget **stub** done; 6/6 stub tests green; WS-vs-REST decision recorded (REST default). **Live round-trip pending** Band creds + identity confirmation (open question #1) — see `docs/credentials-setup.local.md`.
- **Goal:** Stand up `band-sdk`, register 3 remote agents on `app.band.ai` (Pro), create a room, and prove a **reply round-trips** (not just a one-way message). Decide persistent-WS vs request-scoped REST. Freezes `G-BAND-CONTRACT` incl. the **reply-collection seam** (G-05).
- **Repo:** tali
- **Branch:** `feat/band-connector`
- **Depends on:** — · **Time-box:** spike by **EOD day 2** (it gates all of Wave 1 and blocks on open-question #1).
- **Files (create/touch):** create `app/agents/band/band_client.py`, `app/agents/band/registration.md` (handles/agent_ids); touch `requirements.txt` (`+band-sdk`), `app/config.py`, `.env.example` (Band creds).
- **Consumes / Produces:** Produces `G-BAND-CONTRACT` — connector seam: send / on_message / read_context **/ reply-collection** (gateway blocks on `correlation_id`/terminal message → `send_reply`); reuses `event_schemas.py` bodies.
- **Default fallback (G-07):** if WS doesn't clearly win, adopt **request-scoped REST posting + a blocking `read_context` poll keyed by `correlation_id`** — keeps Flask synchronous, no persistent `await agent.run()`.
- **Do-not-touch:** the three agent classes (ported in Phase 2).
- **DoD (self-verifiable):** a script posts via the gateway, a 2nd agent receives via `@mention`, **and the reply is collected back and returned to a synchronous caller** (Phase 1 verification #3); WS-vs-REST decision recorded in `04`.

## Phase 2 — Port agents onto Band, retire the broker

### WP-03 — Port Intake agent onto Band

- **Goal:** Replace `BandSDK.publish("intake_to_ledger"/"cfo_escalation")` with Band send/@mention; adopt `get_client("intake")`.
- **Repo:** tali · **Branch:** `feat/band-intake`
- **Depends on:** `G-BAND-CONTRACT`, `G-MODEL-ROUTER`
- **Files (create/touch):** `app/agents/agent_1_intake.py`.
- **Consumes / Produces:** consumes connector + router; produces `@Ledger`/`@CFO` messages.
- **Do-not-touch:** `agent_2_ledger.py`, `agent_3_cfo.py` (sibling WPs).
- **DoD:** Intake posts to the room as a distinct participant; no `BandSDK` import remains; **the test seam patching `IntakeAgent`'s `BandSDK.publish` is re-pointed to `band_client` and green** (G-08).

### WP-04 — Port Ledger agent onto Band + emit "proposed write" envelope

- **Goal:** Port Ledger off `BandSDK.publish("ledger_updates"/"ledger_errors")`; emit a pre-commit "proposed write" envelope for the Compliance review (WP-07).
- **Repo:** tali · **Branch:** `feat/band-ledger`
- **Depends on:** `G-BAND-CONTRACT`, `G-MODEL-ROUTER`
- **Files (create/touch):** `app/agents/agent_2_ledger.py`.
- **Consumes / Produces:** consumes Intake messages; produces ledger-update + proposed-write messages.
- **Do-not-touch:** sibling agent files.
- **Two-phase commit (G-14):** Ledger commits inline today. For Compliance (WP-07) to *reject pre-commit*, split into **propose → (await review) → commit** over the async room — a re-entrant suspend/resume keyed by `correlation_id` (same async-bridge family as G-05). The reject must actually prevent the DB commit, not just annotate it.
- **DoD:** Ledger reads/writes via the room; a proposed-write envelope is published **and the DB commit is withheld until Compliance approves**; test seam re-pointed + green (G-08).

### WP-05 — Port CFO agent onto Band

- **Goal:** Port CFO off the `ledger_updates`/`cfo_escalation`/`ledger_errors` subscriptions onto room handlers; adopt `get_client("cfo")` for synthesis.
- **Repo:** tali · **Branch:** `feat/band-cfo`
- **Depends on:** `G-BAND-CONTRACT`, `G-MODEL-ROUTER`
- **Files (create/touch):** `app/agents/agent_3_cfo.py`.
- **Do-not-touch:** sibling agent files.
- **Reply ownership (G-11):** decide with WP-02 whether CFO's terminal reply is collected by the gateway (preferred) or CFO keeps calling `send_reply` directly; account for the already-split out-of-band paths (dead-letter saga `agent_3_cfo.py:159`, statement documents).
- **DoD:** CFO composes the user-facing reply from room context; no `BandSDK` import remains; test seam re-pointed + green (G-08).

### WP-06 — Webhook→room gateway; delete the in-memory broker

- **Goal:** Replace `AgentRouter.route` with a gateway that drops the inbound WhatsApp message into the Band room; delete `app/agents/band_sdk.py`.
- **Repo:** tali · **Branch:** `feat/band-gateway`
- **Depends on:** WP-03, WP-04, WP-05
- **Files (create/touch):** `app/web/whatsapp.py`, `app/agents/agent_router.py` (replace), `app/agents/transaction_agent.py` (retire its `AgentRouter().route()` fallback `:31-33`), remove `app/agents/band_sdk.py`.
- **Grep-clean (G-09):** grep **both** `BandSDK` importers **and `AgentRouter` callers** before deleting — `transaction_agent.py` is the known hidden dependent.
- **DoD:** Phase 2 verification #1–3 — "Sold rice 5000" works end-to-end with `band_sdk.py` deleted; reply arrives over WhatsApp; room shows 3 participants; **migrated test suite green** (G-08). Rollback: `BAND_BACKEND=stub`.

## Phase 3 — Collaboration loop + regulated-track audit (sketch)

### WP-07 — Compliance/Reviewer agent (the review leg)

- **Goal:** New 4th agent on a Featherless model that reviews Ledger's proposed-write envelope **before commit** (duplicate / threshold / anomaly checks reusing `_get_evaluated_thresholds`); approve / reject / escalate. Converts chain → plan→execute→review.
- **Repo:** tali · **Branch:** `feat/compliance-agent`
- **Depends on:** WP-04, `G-MODEL-ROUTER`
- **Files (create/touch):** create `app/agents/compliance_agent.py`; register a 4th Band agent (WP-02 registration).
- **DoD:** Phase 3 verification #1 — a flagged write is rejected pre-commit, visible in the room.

### WP-08 — Human-in-the-loop approval in the room

- **Goal:** Route confirm-before-write (`_store_pending`) + `review_queue` as a human-participant approval inside the Band room.
- **Repo:** tali · **Branch:** `feat/human-in-loop`
- **Depends on:** WP-06
- **Files (create/touch):** `app/agents/agent_1_intake.py` (pending hooks — **re-touches the file WP-03 ported; merge after WP-03**, G-10), gateway, a room participant for the human.
- **DoD:** Phase 3 verification #2 — a human approve/reject in the room gates a pending write.

### WP-09 — Unified audit-trail surfacing (Regulated track)

- **Goal:** Join `ai_logs`/`processed_events`/`correlation_id` to Band's unified audit trail; expose a lifecycle/traceability view keyed by `correlation_id`.
- **Repo:** tali · **Branch:** `feat/audit-trail`
- **Depends on:** WP-06
- **Files (create/touch):** create `app/services/audit.py`; a read-only view/route.
- **DoD:** Phase 3 verification #3 — reconstruct one transaction's full lifecycle (model, cost, handoffs, approval) from its `correlation_id`.

### WP-10 — FinOps per-model cost attribution

- **Goal:** Extend cost logging across the three providers; per-agent / per-model spend view (proves deliberate credit optimization).
- **Repo:** tali · **Branch:** `feat/finops`
- **Depends on:** WP-01, WP-09
- **Files (create/touch):** `app/agents/reporting_agent.py` (finops), `app/services/model_router.py` (cost metadata).
- **DoD:** FinOps report shows spend split by provider+agent for a session.

## Phase 4 — Submission polish (stub)

### WP-11 — Deploy, public repo, 5-min demo video

- **Goal:** Deploy the Band-integrated build; make the repo public; record a <5-min demo around the live Band room (plan→execute→review→human-approve→audit). MIT licence added by the user.
- **Repo:** tali · **Branch:** `chore/submission`
- **Depends on:** WP-06 (must-ship); WP-07/08/09 (differentiators, include if landed)
- **Process model (G-12):** the app is pure-sync WSGI with no Procfile/Dockerfile. If WP-02 picked **REST** (default), it deploys inside the existing Flask app. If WP-02 picked **persistent WS agents**, add a worker/supervisor process model (separate long-running process) here.
- **Doc rewrite (G-13):** rewrite `docs/agent_setup.md` §4 (currently calls Band an "in-memory pub/sub broker") to describe the real Band room + `@mention` coordination — it's public + judge-facing.
- **Files (create/touch):** `docs/` (incl. `agent_setup.md`), `README.md`, deploy config (Procfile/Dockerfile if WS), demo script.
- **DoD:** live URL + public repo + video under 5 min / 300 MB submitted on lablab.ai; `agent_setup.md` no longer describes an in-memory broker.
